# FilmDiary — персональный кино-трекер с системой рекомендаций

## Описание
Fullstack приложение для ведения структурированного дневника просмотров и оценки фильмов. Включает в себя получение персональных рекомендаций без необходимости социальной сети.

Представляет собой оптимальную комбинацию кинодневника, персональных рекомендаций и календарного планирования.

Проект включает расширенный функционал: API интеграция для ежедневного обновления рекомендаций и получения расширенной информации о фильмах, Telegram-уведомления, Celery-задачи, активацию аккаунта по email и строгую систему прав доступа.

### Особенности проекта:
- приложение одно-пользовательское: пользователь не видит других пользователей;
- учёт просмотренного (без привязки к предпочтениям других пользователей);
- личные отзывы (личная система хранения отзывов без необходимости публикации);
- личные оценки являются приоритетными над внешними рейтингами: в системе приоритетов личная оценка фильма стоит выше общего рейтинга фильмов международных рейтинговых систем;
- календарь просмотров;
- простая прозрачная рекомендательная система оценки фильмов на основе графов: фильмы и атрибуты (жанры, актёры, режиссёры) выступают узлами, а оценки пользователя формируют веса ребёр).

**смотрю → оцениваю → получаю рекомендации → планирую следующее**

## 🚀Основной функционал

Проект сочетает Django, DRF и Ajax

### Проект реализует:
- Регистрацию и авторизацию пользователей
- Активацию профиля по email через Celery (отложенная задача)
- Интеграцию с Telegram для рассылки уведомлений
- Автоматические Celery-задачи:
  - отправка писем активации профиля
  - отправка напоминаний о запланированных просмотрах фильмов
  - ежедневное обновление индивидуальных рекомендаций фильмов на основе собственных оценок пользователя и метаданных фильмов (жанр, режиссёр, актёры)
- Систему прав доступа
- Пагинацию
- CORS для работы с фронтендом (реализовано ?)
- Покрытие тестами на 80%
- Документацию для части DRF

### Технологии
Python 3.12

Django 5.2.8

Django REST Framework 3.16.1

Celery 5.6.0 + Redis 7.1.0

Requests 2.32.5

PostgreSQL

ZoneInfo (таймзоны)

Coverage (80%)

Flake8 (100%, исключая миграции), isort

## 🔧 Установка и локальный запуск
1. Клонируйте репозиторий
````
git clone https://github.com/nadezhdapopova-spec/FilmDiary.git
cd FilmDiary
````
2. Установите зависимости
````
pip install -r requirements.txt
или
poetry install
````
3. Заполните .env
````
...
````
4. Выполните миграции
````
python manage.py migrate
````
5. Запустите сервер
````
python manage.py runserver
````
6. Запустите Celery, celery-beat
````
celery -A config worker -l INFO (для Windows - celery -A config worker -l INFO -P solo)
celery -A config beat -l INFO
````
## Архитектура проекта
### Модели
**CustomUser**: пользователь
**Film**: фильм - универсальная сущность для всех пользователей
...

### Валидаторы

- ...

### Права доступа

- Неавторизованный пользователь ...
- Авторизованный пользователь ...
- Суперпользователь ...
- Менеджер ...
- Заблокированный пользователь ...

### Интеграция с Telegram
**Проект отправляет сообщения через Telegram Bot API:**
- уведомление о запланированном на текущий день просмотре: в 12:00 согласно таймзоне пользователя
- пользователь получает уведомление один раз: при успешной отправке уведомление не дублируется
````
Пример сообщения:

« ... »
````

### Celery задачи
1. **send_habit_reminder + send_telegram_message**

Ежедневно проверяет:
- текущее время по таймзоне пользователя
- периодичность
- дату создания привычки

При совпадении условий отправляет напоминание в Telegram.

2. **send_activation_email**

Отправляет ссылку активации при регистрации нового пользователя.

Реализовано через отложенную Celery-задачу.

### Тестирование

**Покрытие тестами:** 80%

**Запуск тестов:**
````
...
````
### Документация DRF части приложения
Сгенерирована автоматически и доступна по адресу:
````
/swagger/
или
/redoc/
````

## Запуск проекта на удаленном сервере

Проект развёртывается на удалённом сервере с помощью Docker Compose и GitHub Actions.

**Адрес сервера с развернутым приложением:** http://homeworks.creepysnakes.su/habits/

### Архитектура

Django + DRF

PostgreSQL

Redis

Celery + Celery Beat

Gunicorn

Nginx

Docker / Docker Compose

GitHub Actions (CI/CD)

### Настройка удалённого сервера

**На сервере должны быть установлены:**
````
sudo apt update
sudo apt install -y docker.io docker-compose-plugin nginx
````

**Дополнительно:**

- пользователь добавлен в группу docker

- вход по SSH-ключу

- открыты порты 80, 443, 22

- проект размещается в директории:
/home/<user>/habitladder

### Переменные окружения

**Файл .env:**

- не коммитится в репозиторий

- используется на сервере и создаётся автоматически в GitHub Actions

### GitHub Secrets

В репозитории → Settings → Secrets and variables → Actions должны быть добавлены:

| Secret                    | Назначение              |
| ------------------------- |-------------------------|
| `DJANGO_SECRET_KEY`       | Django SECRET_KEY       |
| `DB_PASSWORD`             | Пароль PostgreSQL       |
| `EMAIL_HOST_USER`         | Почта                   |
| `EMAIL_HOST_PASSWORD`     | Пароль почты            |
| `TELEGRAM_TOKEN`          | Telegram token          |
| `BASE_SERVER_URL`         | Домен или IP сервера    |
| `DOCKER_HUB_USERNAME`     | Docker Hub username     |
| `DOCKER_HUB_ACCESS_TOKEN` | Docker Hub access token |
| `SSH_KEY`                 | Приватный SSH-ключ      |
| `SSH_USER`                | Пользователь сервера    |
| `SERVER_IP`               | IP сервера              |

### CI/CD (GitHub Actions)

Workflow расположен в .github/workflows/ci_cd.yaml

**Алгоритм workflow:**
- Lint
    - запуск линтеров

````
lint:
	isort . --check-only --diff
	black . --check
	flake8 .

format:
	isort .
	black .
````

- Tests

    - запуск Django-тестов

    - при ошибках деплой останавливается


- Build

    - сборка Docker-образа

    - push в Docker Hub


- Deploy

    - генерация .env

    - подмена server_name в nginx.conf

    - деплой на сервер через rsync

    - Cleanup .env из GitHub Actions runner

    - загрузка образов сервисов, указанных в docker-compose.yaml (docker compose pull)

    - запуск сервисов в фоновом режиме (docker compose up -d)

Pre-commit с линтерами запускается при каждом commit.

Workflow запускается автоматически при каждом push.

### Деплой приложения

Деплой происходит автоматически после успешного прохождения тестов.

Ручной деплой на сервере:
````
cd ~/habitladder
docker compose pull
docker compose up -d
````

## Автор
Надежда Попова

Python Developer

📧 nadezhdapopova13@yandex.ru

🔗 GitHub: nadezhdapopova-spec
